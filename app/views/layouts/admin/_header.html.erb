<nav class="navbar navbar-default navbar-fixed-top mgv-navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-brand">
      <p>LOGO Admin</p>
    </div>
    <% if current_user %>
      <div id="navbar" class="navbar-collapse collapse">
        <%= link_to "Packs", admin_packs_path, class: "btn btn-success" %>
        <%= link_to "Orders", admin_orders_path, class: "btn btn-success" %>
        <div class="navbar-form navbar-right">
          <% if current_user %>
            <div class="dropdown">
              <button class="btn btn-success dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-expanded="true">
                <%= current_user.email %>
                <span class="caret"></span>
              </button>
              <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
                <li role="presentation"><%= link_to t('admin.layouts.signed_out'), logout_path, method: :delete %></li>
                <li role="presentation"><%= link_to t('admin.layouts.edit_account'), edit_admin_user_path(current_user.id) %></li>
              </ul>
            </div>
          <% else %>
            <%= link_to "Se connecter", login_path(locale: nil), class: "btn btn-success"  %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</nav>
